{
  "name": "xlsx-template-ex-mod",
  "version": "0.0.1",
  "description": "The template engine for generate xlsx documents",
  "private": false,
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "npm run build && node ./dist/example.js",
    "start-dev": "shx node $NODE_DEBUG_OPTION -r ts-node/register ./src/example.ts dev",
    "build": "shx rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "exceljs": "^1.13.0",
    "lodash": "^4.17.15",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@types/node": "^10.14.10",
    "nodemon": "^1.19.1",
    "shx": "^0.3.3",
    "ts-node": "^7.0.1",
    "typescript": "^3.5.2"
  }
}
